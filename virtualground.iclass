class Room {
    public void Room(string name);
    serverwrite string name;
    publicread User[] users;
    public void joinRoom();
}

class User {
    public void User(string name);
    serverwrite string name;
    private void move(int x, int y);
}